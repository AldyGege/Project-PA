<!DOCTYPE html>
<html lang="id">
<head>
    <%- include('../partials/__head.ejs') %>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        :root {
            --primary-color: #8B6F1A;
            --button-color: #8B6F1A;
            --background-color: #ffffff;
        }
        
        body {
            background-color: var(--background-color);
            margin: 0;
            padding: 0;
            font-family: sans-serif;
        }
        
        .header {
            background-color: var(--primary-color);
            padding: 10px 20px;
            display: flex;
            align-items: center;
            justify-content: space-between;
        }
        
        .header .home-icon {
            font-size: 20px;
            color: white;
            text-decoration: none;
        }
        
        .header .cancel-button {
            background: #E6D5B8;
            color: red;
            border: none;
            padding: 5px 15px;
            border-radius: 15px;
        }
        
        .branding {
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 15px;
            flex-wrap: wrap;
            text-align: center;
        }
        
        .logo {
            height: 50px;
            margin-right: 10px;
        }
        
        .branding-title {
            color: #8B6F1A;
            font-size: 22px;
            font-weight: bold;
        }
        
        /* Form Container */
        .form-container {
            max-width: 900px;
            background: #FDF8F2;
            border-radius: 10px;
            padding: 30px;
            margin: 40px auto;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1);
        }
        
        .form-group input,
        .form-group select {
            width: 100%;
            padding: 10px;
            border-radius: 25px;
            border: 1px solid #ccc;
            outline: none;
            box-sizing: border-box;
        }
        
        .form-row {
            display: flex;
            gap: 15px;
            flex-wrap: wrap;
        }
        
        .form-row .form-group {
            flex: 1;
            min-width: 100px;
        }
        
        .checkbox-container {
            display: flex;
            align-items: center;
            justify-content: space-between;
            flex-wrap: wrap;
            gap: 10px;
            margin-top: 10px;
        }
        
        .checkbox-container input[type="checkbox"] {
            width: 18px;
            height: 18px;
            accent-color: var(--primary-color);
            cursor: pointer;
        }
        
        .checkbox-container label {
            font-size: 14px;
            color: #333;
            flex: 1;
        }
        
        .submit-button {
            background-color: var(--button-color);
            color: white;
            padding: 12px 24px;
            border: none;
            border-radius: 30px;
            cursor: pointer;
            font-size: 16px;
            font-weight: bold;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            justify-content: center;
            width: 200px;
            margin-left: auto;
        }
        
        .submit-button:hover {
            background-color: #6E550F;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.2);
        }
        
        .konsultasi-section {
    background-color: #8B6F1A;
    padding: 40px 20px;
    color: white;
}

.konsultasi-content {
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-wrap: wrap;
    max-width: 1200px;
    margin: 0 auto;
    gap: 20px;
}

.konsultasi-text {
    flex: 1;
    min-width: 250px;
}

.konsultasi-text h2 {
    margin: 0 0 10px;
    font-size: 28px;
    font-weight: bold;
}

.konsultasi-text p {
    font-size: 16px;
    margin: 0;
}

.konsultasi-button {
    flex-shrink: 0;
}

.hubungi-kami-btn {
    background-color: #7ED957;
    color: white;
    padding: 12px 24px;
    border-radius: 30px;
    text-decoration: none;
    font-weight: bold;
    font-size: 16px;
    transition: background 0.3s ease;
}

.hubungi-kami-btn:hover {
    background-color: #69c647;
}

/* Responsif */
@media (max-width: 768px) {
    .konsultasi-content {
        flex-direction: column;
        text-align: center;
    }

    .konsultasi-button {
        margin-top: 20px;
    }
}

        /* ---------------- RESPONSIVE ---------------- */
        
        @media (max-width: 768px) {
            .form-row {
                flex-direction: column;
            }
        
            .submit-button {
                width: 100%;
                margin-left: 0;
                margin-top: 15px;
            }
        
            .branding {
                flex-direction: column;
                text-align: center;
            }
        
            .logo {
                height: 40px;
                margin-right: 0;
                margin-bottom: 10px;
            }
        
            .branding-title {
                font-size: 18px;
            }
        
            .form-container {
                margin: 20px 10px;
                padding: 20px;
            }
        
            .checkbox-container {
                flex-direction: column;
                align-items: flex-start;
            }
        }
        
    </style>
</head>
<body>
    <div class="header">
        <a href="/users" class="home-icon">&#8962;</a>
        <!-- <button class="cancel-button">Batalkan</button> -->
    </div>

    <!-- Tambahkan bagian baru untuk logo & teks -->
<div class="branding">
    <img src="../../images/logo-madrasah.png" alt="" class="logo">
    <h2 class="branding-title">Portal Pendaftaran Mandiri Online Madrasah At-Taqwa</h2>
</div>

<% if (masaPendaftaran && aktifPendaftaran) { %>
  <div class="form-container">
    <form action="/pendaftaran/store" method="POST" enctype="multipart/form-data" class="pt-3">
        <h5><b>Informasi Pribadi</b></h5>
        <%- include('../partials/__messagess.ejs') %>
        <div class="form-row">
            <div class="form-group">
                <input type="text" placeholder="Masukkan Nama Peserta" name="nama_pendaftar" required>
                <small>Nama Lengkap</small>
            </div>
            <input type="hidden" name="id_users" value="<%= data2.id_users %>">
            <div class="form-group">
                <input type="file" name="gambar_pendaftar" id="gambar_pendaftar" required>
                <small>Foto Siswa</small>
            </div>
        </div>

        <div class="form-row">
            <div class="form-group">
                <input type="date" placeholder="Masukkan Tempat, Tanggal Lahir" name="ttl" required>
                <small>Tempat, Tanggal Lahir</small>
            </div>
            <div class="form-group">
                <select name="gender" required>
                    <option value="">-- Pilih Jenis Kelamin --</option>
                    <option value="Pria">Laki-laki</option>
                    <option value="Wanita">Perempuan</option>
                </select>
                <small>Jenis Kelamin</small>
            </div>
        </div>

        <div class="form-row">
            <div class="form-group">
                <input type="text" placeholder="Masukkan NIK" name="nik" required>
                <small>No Induk Keluarga (NIK)</small>
            </div>
            <div class="form-group">
                <input type="text" placeholder="Masukkan Alamat Lengkap" name="alamat_pendaftar" required>
                <small>Alamat Lengkap</small>
            </div>
        </div>

        <h5><b>Informasi Orang Tua/Wali</b></h5>
        <div class="form-row">
            <div class="form-group">
                <input type="text" placeholder="Masukkan Nama Orang Tua/Wali" name="nama_ortu_pendaftar" required>
                <small>Nama Lengkap Orang Tua/Wali</small>
            </div>
            <div class="form-group">
                <input type="text" placeholder="Masukkan Pekerjaan Orang Tua/Wali" name="pekerjaan_ortu_pendaftar" required>
                <small>Pekerjaan</small>
            </div>
        </div>

        <div class="form-row">
            <div class="form-group">
                <input type="text" placeholder="Masukkan No Telepon Orang Tua/Wali" name="no_telp_ortu_pendaftar" required>
                <small>No Telepon</small>
            </div>
            <div class="form-group">
                <input type="text" placeholder="Masukkan Alamat (Jika Berbeda)" name="alamat_ortu_pendaftar">
                <small>Alamat (Jika Berbeda)</small>
            </div>
        </div>

        <input type="hidden" id="waktu_pendaftaran" name="waktu_pendaftaran">

        <div class="checkbox-container">
            <div>
                <input type="checkbox" id="agree" required>
                <label for="agree">Saya Menyatakan bahwa Data yang diisikan Adalah Benar</label>
            </div>
            <button type="submit" class="submit-button">Daftar</button>
        </div>
    </form>
</div>
<% } else { %>
  <div class="form-container text-center">
    <h4>Pendaftaran belum dibuka atau Sudah Ditutup</h4>
    <% if (masaPendaftaran) { %>
      <p>Jadwal Pendaftaran:</p>
      <p><strong>
        <%= new Date(masaPendaftaran.tanggal_mulai).toLocaleDateString('id-ID', { day: '2-digit', month: 'long', year: 'numeric' }) %>
      </strong> sampai <strong>
        <%= new Date(masaPendaftaran.tanggal_akhir).toLocaleDateString('id-ID', { day: '2-digit', month: 'long', year: 'numeric' }) %>
      </strong></p>
    <% } else { %>
      <p>Jadwal pendaftaran belum tersedia.</p>
    <% } %>
  </div>
<% } %>

<!-- Section Konsultasi (Horizontal Layout) -->
<div class="konsultasi-section">
    <div class="konsultasi-content">
        <div class="konsultasi-text">
            <h2>Mau Konsultasi ?</h2>
            <p>Tanyakan Apa Saja Kepada Service dan Dapatkan Solusi Terbaik untuk Masalah Anda!</p>
        </div>
        <div class="konsultasi-button">
            <a href="/kontak" class="hubungi-kami-btn">Hubungi Kami</a>
        </div>
    </div>
</div>

    <script>
        // Mengisi waktu_pendaftaran secara otomatis
        document.addEventListener("DOMContentLoaded", function () {
            let now = new Date();
            let formattedDate = now.toLocaleString("en-CA", { 
                year: "numeric", 
                month: "2-digit", 
                day: "2-digit", 
                hour: "2-digit", 
                minute: "2-digit", 
                second: "2-digit",
                hour12: false 
            }).replace(",", ""); // Menghilangkan koma antara tanggal & waktu
        
            document.getElementById("waktu_pendaftaran").value = formattedDate;
        });
        
    </script>
</body>
</html>